import 'package:flutter/material.dart';

class CoustomerHomePage extends StatelessWidget {
  const CoustomerHomePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Customer Home Page'),
      ),
      body: Center(
        child: Text('Customer Home Page'),
      ),
    );
  }
}
